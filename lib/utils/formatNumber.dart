import 'package:flutter/material.dart';

class FormatNumber extends StatelessWidget {
  final double number;
  const FormatNumber(this.number, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
